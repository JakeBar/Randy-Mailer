G'day, <%= @user.login %>
===============================================

Today's surf report rating:

<%= @user.surf_data[0]['solidRating'] %>/5. That's pretty good!

Basic Stats
===============================================

<% @user.surf_data.each do |p| %>
Min/Max Wave Ht (ft) - <%= p['swell']['minBreakingHeight'] %>/<%= @user.surf_data[0]['swell']['minBreakingHeight'] %>, Wind Direction - <%= p['swell']['components']['combined']['compassDirection'] %>, Temperature - <%= p['condition']['temperature']%> <%= @user.surf_data[0]['condition']['unit'] %>
Reported at - <%= Time.at(@user.surf_data[0]['localTimestamp']).to_time %>
<% end %>

Contact Information / Feedback
===============================================

Got a suggestion or feedback for this report? Send an email to <%= @user.email %>.

Have a great day!
Love - Randy, Surf Mailer